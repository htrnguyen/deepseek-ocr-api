# Project Map — deepseek-ocr-api

> Auto-generated by `project-scanner` at 2026-04-22T20:20:06  
> **Stats**: 4 dirs, 18 files, 10 Python files, ~1127 lines of Python

## Directory Tree

```
deepseek-ocr-api/
├── .code-review-graph/
│   ├── .gitignore
│   └── graph.db
├── DocLayout-YOLO-DocStructBench/
│   └── doclayout_yolo_docstructbench_imgsz1024.pt
├── test/
│   └── needs-improvement-handwritten-version-jpg.jpg
├── utils/
│   ├── exceptions.py  # 2 classes
│   ├── image_processor.py  # ImageProcessor (1m)
│   ├── loop_detector.py  # LoopDetector (1m)
│   └── post_processor.py  # OCRPostProcessor (2m)
├── .env.example  # 11 lines
├── config.py  # Settings (0m)
├── doclayout_service.py  # DocLayoutService (3m)
├── logger.py  # setup_logger
├── main.py  # routes
├── needs-improvement-handwritten-version-jpg (1).jpg
├── ocr_service.py  # DeepSeekOCRService (6m)
├── paddle_detect_service.py  # PaddleDetectService (3m)
├── README.md  # DeepSeek OCR API
└── requirements.txt  # 13 lines
```

## Module Summary

### `main.py`

- **Routes**: `deepseek_ocr`, `paddle_ocr`, `paddle_detect`, `health`
- **Functions**: `async lifespan`
- **Deps**: asyncio, config, contextlib, doclayout_service, fastapi, logger

### `doclayout_service.py`

- **class `DocLayoutService`** (3 methods)
  - `detect_figures`
- **Deps**: asyncio, config, doclayout_yolo, fastapi, logger, os

### `ocr_service.py`

- **class `DeepSeekOCRService`** (6 methods)
  - `process`
- **Deps**: asyncio, config, fastapi, logger, ollama, os

### `paddle_detect_service.py`

- **class `PaddleDetectService`** (3 methods)
  - `detect`
- **Deps**: asyncio, config, fastapi, logger, os, threading

### `config.py`

- **class `Settings`** (0 methods)
- **Deps**: pydantic, pydantic_settings

### `utils/exceptions.py`

- **class `TokenLoopError`** (1 methods)
- **class `EmptyOutputError`** (1 methods)

### `utils/image_processor.py`

- **class `ImageProcessor`** (1 methods)
  - `preprocess_image`
- **Deps**: PIL, io, logger, tempfile

### `utils/loop_detector.py`

- **class `LoopDetector`** (1 methods)
  - `detect`
- **Deps**: logger, re

### `utils/post_processor.py`

- **class `OCRPostProcessor`** (2 methods)
  - `extract_bboxes`, `clean`
- **Deps**: re

## Tech Stack

| Component    | Version     |
| ------------ | ----------- |
| Dependencies | 12 packages |
| fastapi      | latest      |
| uvicorn      | [standard]  |

## Configuration (.env)

| Variable                    | Purpose          |
| --------------------------- | ---------------- | --------- | ---------------------------------- |
| `OLLAMA_TIMEOUT`            |                  |
| `OLLAMA_KEEPALIVE_INTERVAL` |                  |
| `OLLAMA_REPEAT_PENALTY`     |                  |
| `OLLAMA_REPEAT_LAST_N`      |                  |
| `RATE_LIMIT`                |                  |
| `DEFAULT_PROMPT`            | DEFAULT_PROMPT=< | grounding | >Convert the document to markdown. |

---

_Generated by project-scanner — re-run anytime to update._
